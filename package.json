{
    "name": "show-scheduler",
    "private": true,
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "wasm-pack build --dev --target web --out-dir ../src/lib/wasm rust && vite dev",
        "build": "wasm-pack build --release --target web --out-dir ../src/lib/wasm rust && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "@sveltejs/vite-plugin-svelte": "^7.0.0",
        "svelte": "^5.55.2",
        "typescript": "^6.0.2",
        "vite": "^8.0.7"
    }
}
